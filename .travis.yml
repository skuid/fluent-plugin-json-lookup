sudo: false
language: ruby

rvm:
  - 2.2.x
  - 2.3.x
  - 2.4.x
  - ruby-head

gemfile:
 - Gemfile
 - Gemfile.v0.12

matrix:
  allow_failures:
    - rvm: ruby-head

before_install:
- gem update bundler
- gem install rubocop

script:
- rubocop
- bundle exec rake test
